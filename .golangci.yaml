version: "2"

linters:
  enable:
    - revive
  settings:
    revive:
      enable-all-rules: false
      rules:
        - name: unused-parameter
        - name: unexported-naming
        - name: var-naming
  exclusions:
    paths:
      - yacc/console/lex.go

run:
  timeout: 10m
